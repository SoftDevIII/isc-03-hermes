{
  "name": "db",
  "version": "1.0.0",
  "description": "",
  "author": "",
  "private": true,
  "license": "UNLICENSED",
  "scripts": {
    "dev": "docker compose down && docker volume rm --force postgres-data-isc-hermes && docker compose up -d",
    "up": "docker compose down && docker volume rm --force postgres-data-isc-hermes && docker compose up -d"
  }
}
